includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 5
    tmpDir: .phpstan-cache

    paths:
        - app

    excludePaths:
        - vendor
        - storage
        - bootstrap/cache

    memoryLimit: 1G

        bootstrapFiles:
            - vendor/autoload.php
